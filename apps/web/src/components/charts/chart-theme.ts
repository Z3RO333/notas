export const CHART_GRID_STROKE = 'hsl(var(--border))'

export const CHART_AXIS_TICK = {
  fontSize: 11,
  fill: 'hsl(var(--muted-foreground))',
}

export const CHART_AXIS_TICK_MD = {
  fontSize: 12,
  fill: 'hsl(var(--muted-foreground))',
}

export const CHART_CATEGORY_TICK = {
  fontSize: 11,
  fill: 'hsl(var(--foreground))',
}

export const CHART_VALUE_LABEL = {
  fontSize: 11,
  fill: 'hsl(var(--foreground))',
}

export const CHART_VALUE_LABEL_SM = {
  fontSize: 10,
  fill: 'hsl(var(--foreground))',
}

export const CHART_LEGEND_STYLE = {
  fontSize: 12,
  color: 'hsl(var(--foreground))',
}
